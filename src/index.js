import React from 'react';
import ReactDOM from 'react-dom';
import ApolloProvder from './ApolloProvider.jsx';

ReactDOM.render(
	<React.StrictMode>
		<ApolloProvder />
	</React.StrictMode>,
	document.getElementById('root')
);
